# Javascript-The-Greek-Pocket-Sized-Guide

Download NodeJS from https://nodejs.org/en/ and install it.



With node installed, run:


```shell
npm install
```

to install dependencies and then execute the example using the following command:

```shell
node index.js
```